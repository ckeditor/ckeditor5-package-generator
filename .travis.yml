dist: jammy
addons:
  chrome: stable
language: node_js
services:
  - xvfb
node_js:
  - node
cache:
  yarn: true
cache:
  - node_modules
before_install:
  - npm i -g yarn
install:
  - yarn install
script:
  - yarn run test
  - yarn run lint
