---
type: Feature
scope:
  - ckeditor5-package-generator
---

Packages created by the generator now declare approved build scripts using pnpm's `allowBuilds` setting in `pnpm-workspace.yaml`, replacing the deprecated `onlyBuiltDependencies` and `ignoredBuiltDependencies` keys. This makes generated packages compatible with pnpm v11.
