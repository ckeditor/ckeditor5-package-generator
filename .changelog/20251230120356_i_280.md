---
type: Minor breaking change
scope:
  - ckeditor5-package-tools
see:
  - 280
---

Removed the `dll:build` script, because the [`ckeditor5-package-generator`](www.npmjs.com/package/ckeditor5-package-generator) package no longer supports old DLL-based installation method.
